# Non-interactive, line-range staging for git (and for coding agents)

Live: https://git-stage-lines.sociobot.in — built by the Param Factory (`cli`).

See `.factory/brief.json` for the researched problem this solves and `.factory/design.md` for the visual system.

## Develop

```
npm install
npm run dev
npm test
npm run build   # -> dist/
```
