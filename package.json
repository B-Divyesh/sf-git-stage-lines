{
  "name": "git-stage-lines-product",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite site",
    "build:site": "vite build site --outDir ../dist/site && node site/generate-sw.mjs",
    "build:cli": "cargo build --release",
    "build": "npm run build:cli && npm run build:site",
    "preview": "node site/tests/server.mjs",
    "test": "cargo test && node --test wrappers/node/test.js && PYTHONPATH=wrappers/python python3 -m unittest discover -s wrappers/python/tests && npm run test:claims && npm run build:site && playwright test",
    "test:site": "playwright test",
    "test:claims": "node site/tests/run-claims.mjs"
  },
  "devDependencies": {
    "@axe-core/playwright": "4.10.2",
    "@playwright/test": "1.58.2",
    "vite": "7.3.6"
  }
}
